# Encrypt all tracked files by default with git-crypt.
* filter=git-crypt diff=git-crypt

# Keep git attributes readable so encryption rules can be applied.
.gitattributes !filter !diff

# Workflow files must remain readable by GitHub to execute Actions.
.github/workflows/** !filter !diff

# Keep setup instructions readable
GIT_CRYPT_SETUP.md !filter !diff